//turning a trolly into the right railroad lane using javascript conditionings
// if the track is clear, then the trolly will go striaght

if (track === "clear") {
    goStraight();
} else {
    turnRight();
}





//lovescore test

let yourName = prompt("What is your name?");
let theirName = prompt("What is your lovers name?")

// Random number between 1 and 100
let lovescore = Math.Random() * 100 + 1;

alert(lovescore)